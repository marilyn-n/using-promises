{
  "name": "using-promises",
  "version": "1.0.0",
  "description": "Some examples using promises with .then() and .catch()",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.2.0"
  }
}
